package com.example.rotativo

import io.flutter.embedding.android.FlutterFragmentActivity

class MainActivity : FlutterFragmentActivity() {
    // Orientation is controlled at Flutter level to avoid conflicts with translucent theme
    // See main.dart for SystemChrome.setPreferredOrientations configuration
}
